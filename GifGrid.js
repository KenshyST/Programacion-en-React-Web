import { useEffect, useState } from "react"

export const GifGrid = ({category}) => {
    console.log(category)

    return(
        <>
        <h3> {category} </h3>
        
        </>
    )
}